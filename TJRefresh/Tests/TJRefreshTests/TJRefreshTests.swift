import XCTest
@testable import TJRefresh

final class TJRefreshTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TJRefresh().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
